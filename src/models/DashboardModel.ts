type DashboardModel = {
  user_aktif: number;
  user_tidak_aktif: number;
  total_user: number;
  user_dokumen: number;
  approval_pending: number;
  total_dokumen: number;
};

export default DashboardModel;
